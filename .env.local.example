# Copy this file to .env.local and fill in the values from your Supabase
# project settings: Project Settings → API.
#
# NEXT_PUBLIC_* variables are exposed to the browser — never put a secret
# in one of them. Only the anon/publishable key belongs here.

# Project URL (Project Settings → API → Project URL)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Public "anon" / "publishable" key (Project Settings → API → Project API keys).
# Newer Supabase projects label this key "publishable" (sb_publishable_...);
# older projects label the same key "anon public" (a long JWT). Either one
# goes here — they play the exact same role.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-anon-or-publishable-key

# Secret "service_role" key (Project Settings → API → Project API keys).
# Server-only. NEVER prefix this with NEXT_PUBLIC_, never expose it to the
# browser, never commit it. Used exclusively in Server Actions to invite,
# update and delete users through the Supabase Auth Admin API.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Public site URL, used to build the redirect links inside invitation and
# password-reset e-mails. Optional locally (falls back to the request's
# Host header) — set it explicitly in Vercel to your production domain.
# NEXT_PUBLIC_SITE_URL=https://preox.example.com

# Symmetric key used to encrypt third-party API keys at rest (AES-256-GCM):
# each À table user's own Gemini/Pexels keys, and El Profesor's shared
# Gemini key. Server-only. Generate with: openssl rand -base64 32
A_TABLE_ENCRYPTION_KEY=your-32-byte-base64-key

# El Profesor's Gemini API key (used for chapter extraction, complément and
# passage-to-block proposals) is configured from the app itself — open
# El Profesor as an admin and use the "Réglages IA" button — not via an
# env var. It's stored encrypted in the database using the key above.

# Optional: transactional email (Resend, https://resend.com). Without these
# two set, the app runs fine but silently skips the "new device" login
# alert and the weekly digest email instead of sending them.
# RESEND_API_KEY=re_your_api_key
# RESEND_FROM_EMAIL=PreOx <notifications@your-verified-domain.com>

# Optional: browser push notifications. Generate a pair with:
#   npx web-push generate-vapid-keys
# Without these set, the push toggle in Profile shows an explanatory error
# instead of a broken subscribe button — nothing else is affected.
# NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-public-key
# VAPID_PRIVATE_KEY=your-private-key

# Required only if you want the weekly email digest to actually fire (see
# vercel.json — Mondays 08:00 UTC). Any random string; Vercel automatically
# sends it back as `Authorization: Bearer $CRON_SECRET` on cron-triggered
# requests to this project once the env var is named exactly this.
# CRON_SECRET=generate-with-openssl-rand-base64-32
